import styled from 'styled-components'
import { cores } from '../../style'

export const Rodpe = styled.div`
  display: block;
  text-align: center;

  p {
    color: ${cores.red};
    font-weight: 400;
    font-size: 10px;
    padding-bottom: 40px;
  }

`
export const Foter = styled.footer`
  background-color: ${cores.white};

`
export const Box = styled.div`
  display: block;
  align-items: center;

  img {
    margin-top: 40px;
  }

`
export const Div = styled.div`
  display: flex;
  justify-content: center;

`
export const ListUl = styled.ul`
  display: flex;

  img {
    margin-right: 8px;
    margin-top: 32px;
    margin-bottom: 80px;

  }
`
